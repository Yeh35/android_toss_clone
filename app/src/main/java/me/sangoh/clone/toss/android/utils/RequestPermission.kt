package me.sangoh.clone.toss.android.utils

import android.Manifest
import android.content.ContextWrapper
import android.content.pm.PackageManager
import android.os.Build
import androidx.annotation.RequiresApi


/**
 * 권한을 체크하고 원한 요청을 한다.
 */
private fun requestPermission(contextWrapper: ContextWrapper, permission: String) {
//    if (contextWrapper.checkSelfPermission(Manifest.permission.ACCESS_FINE_LOCATION)
//        == PackageManager.PERMISSION_GRANTED) {
//        showDialog("Permission granted")
//    } else {
//        requestPermissions(arrayOf(Manifest.permission.ACCESS_FINE_LOCATION), // 1
//            PERMISSION_REQUEST_CODE) // 2
//    }
}